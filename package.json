{
  "name": "nodejs-wss-tunnel",
  "description": "A self-hosted HTTP reverse tunnel built on WebSocket with a compact binary protocol. Expose Stable Diffusion, Ollama, ComfyUI or any local service to the internet. Features backpressure management, HMAC-SHA256 admin endpoints, and one-command client deployment.",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prod": "NODE_ENV=production node src/index.js",
    "dev": "node serve/build.js && NODE_ENV=development node src/index.js",
    "test": "NODE_ENV=test node --test $(find test -name '*.test.js')",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "audit:commits": "node scripts/audit-commits.js",
    "test:protocol-negative": "NODE_ENV=test node --test test/tcp/protocol-negative.test.js",
    "build:client": "node serve/build.js",
    "check": "yarn lint && yarn test && yarn build:client"
  },
  "packageManager": "yarn@4.17.1",
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "dotenv": "^17.4.2",
    "esbuild": "^0.28.1"
  }
}
